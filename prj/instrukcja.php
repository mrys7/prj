<!DOCTYPE html>
<html lang="en">
 <head>
   <meta charset="UTF-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   <link href='https://fonts.googleapis.com/css?family=Raleway' rel='stylesheet'>
   <link rel="stylesheet" href="style.css" />
   <title>prj</title>
 </head>
 <body>
     <?php
include 'nawigacja.php';
?>
   <section class="content-inst">
   <center>
   <img id="en" src="images/haloen.png">
   <img id="pl" src="images/halo.png">
   <p id="pl">Użytkownik aby korzystać z funkcjonalności systemu musi posiadać własne konto oraz być zalogowanym. Aby się zarejestrować, z menu należy kliknąć w przycisk Rejestracja który przeniesie użytkownika do formularza, wypełnić go oraz przesłać, 
a następnie kliknąć przycisk Logowanie aby przejść do formularza za pomocą którego można zalogować się do systemu.</p>
<p id="en">To use the system’s functionality, the user must have his own account and be logged in. To register, click the Registration button from the menu which will take user to the form. User should fill it, submit it and then click the Login button so user will be redirected to the form which will grant access to the system.</p>
<img id="en" src="images/rejestracjaen.png">
<img id="pl" src="images/rejestracja.png"><br>
<img id="en" src="images/logowanieen.png">
<img id="pl" src="images/logowanie.png">

<p id="pl">W celu wyszukania zestawu do nauki z menu należy kliknąć przycisk Wyszukaj zestawu, a następnie wpisać ciąg znaków na podstawie którego zostaną wyszukane zestawy do nauki. Aby zacząć przeglądać dany zestaw należy kliknąć przycisk Wyświetl znajdujący się z prawej strony od nazwy zestawu.</p>
<p id="en">In order to search for a learning set, click the Search for a set button from the menu and then enter the string of characters based on which sets will be searched for. To view given set, click the View button to the right of the set name.</p>
<img id="en" src="images/wyszukajen.png">
<img id="pl" src="images/wyszukaj.png"><br>
<img id="en" src="images/listazestawowen.png">
<img id="pl" src="images/listazestawow.png">


<p id="pl">Podczas przeglądania zestawu użytkownik może skorzystać z poniższych funkcji poprzez kliknięcie odpowiednich przycisków:</p>
<p id="en">While viewing the set, user can use the following functions by clicking the appropriate buttons:</p>
<img id="en" src="images/przegladanieen.png">
<img id="pl" src="images/przegladanie.png">

<p id="pl">Zapisz zestaw - po skorzystaniu z tej funkcji, dany zestaw zostanie dopisany do listy zapisanych zestawów użytkownika</p>
<p id="en">Save set- po after using this function, given set will be added to the user list of saved sets</p>
<p id="pl">Moduł słówek - za pomocą tej funkcjonalności użytkownik może przeglądać słówka z zestawu (najpierw słowo, a po kliknięciu przycisku Następne jego tłumaczenie)</p>
<p id="en">Vocabulary module - using this functionality, user can view the words from the set (firstly the word and after click the Next button its translation)</p>
<img id="en" src="images/slowkaen.png">
<img id="pl" src="images/slowka.png">

<p id="pl">Moduł testu - jeżeli dany zestaw ma stworzony test, użytkownik będzie mógł go wypełnić oraz przesłać do systemu do sprawdzenia</p>
<p id="en">Test module - if a given set has a test created, user will be able to complete it and send it to the system for checking</p>
<img id="en" src="images/testen.png">
<img id="pl" src="images/test.png">


<p id="pl">Zalogowany użytkownik może przejść na stronę swojego profilu za pomocą przycisku Profil, na której ma dostępne poniższe funkcje:</p>
<p id="en">Logged in user can see his profile page using the Profile button where he has the following functions available:</p>
<img id="en" src="images/profilen.png">
<img id="pl" src="images/profil.png">

<p id="pl">Stwórz własny zestaw - po kliknięciu użytkownik będzie przeniesiony na stronę tworzenia zestawu. Za pomocą przycisku Dodaj pole użytkownik może dodać dodatkowe pole do wprowadzania danych.</p>
<p id="en">Create your own set - the user will be redirected to the set creation page. Using the Add field button user can add additional input field.</p>
<img id="en" src="images/dodajzestawen.png">
<img id="pl" src="images/dodajzestaw.png">

<p id="pl">Zapisane zestawy - użytkownik zobaczy listę zapisanych zestawów. Za pomocą przycisku Wykreśl zestaw można usunąć dany zestaw z listy zapisanych zestawów</p>
<p id="en">Saved sets - user will see a list of saved sets. User can remove a given set from the list by clicking on Remove set button</p>
<img id="en" src="images/zapisaneen.png">
<img id="pl" src="images/zapisane.png">

<p id="pl">Twoje zestawy - zostaje wyświetlona lista zestawów których autorem jest zalogowany użytkownik. Do dyspozycji użytkownika są funkcje:</p>
<p id="en">Your sets - this option display a list of sets that logged user has created. The following function are available:</p>
<img id="en" src="images/twojezestawyen.png">
<img id="pl" src="images/twojezestawy.png">

<p id="pl">Edytuj zestaw - użytkownik może edytować wybrany zestaw</p>
<p id="en">Edit set - user can edit selected set</p>
<p id="pl">Stwórz test - użytkownik będzie przeniesiony na stronę tworzenia testu. Za pomocą przycisku Dodaj pole użytkownik może dodać dodatkowe pole do wprowadzania danych. Za pomocą okrągłego pola wyboru użytkownik zaznacza poprawną odpowiedź</p>
<p id="en">Create a test - user will be redirected to the test creation page. Using Add field button user can add additional input field. User selects correct answer by click on the circular selection field</p>
<img id="en" src="images/nowytesten.png">
<img id="pl" src="images/nowytest.png">

<p id="pl">Skasuj test - za pomocą tego przycisku użytkownik kasuje test</p>
<p id="en">Delete test - user deletes test if exists</p>
<p id="pl">Skasuj zestaw - użytkownik kasuje wybrany zestaw do nauki</p>
<p id="en">Delete set - user deletes selected learning set</p><br><br><br>
   </center>
   </section>
 </body>
</html>
<script>
        var polish = document.getElementById('pl_click'),
    english = document.getElementById('en_click'),
    pl_txt = document.querySelectorAll('#pl'),
    en_txt = document.querySelectorAll('#en'),
    nb_pl = pl_txt.length,
    nb_en = en_txt.length;

polish.addEventListener('click', function() {
    langue(polish,english);
}, false);

english.addEventListener('click', function() {
    langue(english,polish);
}, false);

function langue(langueOn,langueOff){
    if (!langueOn.classList.contains('current_lang')) {
        langueOn.classList.toggle('current_lang');
        langueOff.classList.toggle('current_lang');
    }
    if(langueOn.innerHTML == 'PL'){
        afficher(pl_txt, nb_pl);
        cacher(en_txt, nb_en);
        polish.style.display = 'none';
        english.style.display = 'block';
         localStorage.setItem('langON', 'en');
         localStorage.setItem('langOFF', 'pl');
    }
    else if(langueOn.innerHTML == 'EN'){
        afficher(en_txt, nb_en);
        cacher(pl_txt, nb_pl);
        english.style.display = 'none';
        polish.style.display = 'block';
        localStorage.setItem('langON', 'pl');
        localStorage.setItem('langOFF', 'en');
    }
}

function afficher(txt,nb){
    for(var i=0; i < nb; i++){
        txt[i].style.display = 'block';
    }
}
function cacher(txt,nb){
    for(var i=0; i < nb; i++){
        txt[i].style.display = 'none';
    }
}
function init(){
   if(!localStorage.getItem('langON')){
        langue(polish, english);
    }
    else{
        if(localStorage.getItem('langON') == 'en')
        langue(polish, english);
        else
        langue(english,polish);
    }
}
init();

</script>